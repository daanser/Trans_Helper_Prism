# plan-m4.md —— M4 灰度与运营（执行计划）

> 状态：**待评审**（2026-09-11 起稿）
> 上游：`tasks.md` 的 M4（T4.1 小圈子内测 + key 演练 + 反馈清零 + 性能表回填 + 月账）、`plan.md` §5.2/§8.4、`history.md` §8.1
> 本文只描述计划；实施时逐包开任务、逐包验收。

---

## 1. 现状基线（M0–M3 已上线，M4 从这里出发）

**功能面（全部线上可用）**
- 四库语义检索（mtf 490 chunk / ftm 79 / rle 836 / mio 76）+ rerank（默认开）+ KV 短期缓存 + embedding 失败自动回退（Qdrant 全文索引）
- 知识树 `/api/v1/tree/:wiki_id`、`/api/v1/corpora`
- 增量摄取：**GitHub Actions 每日 UTC 02:00**，**真·文件级增量**（git blob sha 比对）；零-chunk 短文件已不再空转（#5）
- AI 伴读：`/search/stream`（SSE）+ `/chat` 多轮（≤10 轮），引用 `[来源n]` 可点击回跳；前端 Markdown 渲染
- 账号：X OAuth2 + PKCE（**DB 只存 `sha256(x_id)`**，不存 X 明文）；无状态 JWT 30 天
- 配额：**滚动 5 小时窗口 + 加权 token**（搜索 200 / +rerank +100 / LLM 按真实 token / 回退 0），
  **按注册时间网格锚定**（重置时刻固定可预测），前端只显示百分比 + 「x 小时后重置」
- 限流：按 IP 分档（CN 家宽 30 / CN 其它 15 / **CN 机房 6** / 境外 10 / 登录 60 次每分钟；
  **LLM = 该档 ÷5 向上取整**）+ 突发（20 次/10 秒 → **硬封 60 秒**）+ 全局匿名软/硬熔断（600 / 1200）
- 观测：`/admin/usage`（配额、**真实 requests**、LLM token、key 行）、`/admin/ratelimit`（分档/scopes/熔断/封禁数）、
  `/admin/audit`、`/admin/ingest/runs`、`/admin/whoami`（诊断）
- 自定义模型（AES-GCM 加密落库，仅本人可用）、管理端封禁/加额/审计
- 域名：主 `search.chengxi.moe`，冗余 `search.transhelper.org`（同一 Pages 项目；`/api/*` 同源反代到 Worker）

**已知的"不是 bug 但要知道"**
| 事项 | 说明 |
|---|---|
| **X 登录在墙内不可用** | 授权页本身是 x.com，被墙；墙内用户只能用匿名检索（`REQUIRE_LOGIN=0`），AI 伴读需要登录 → 墙内用不了 AI |
| 反代延迟尖峰 | Worker 自报 2.7–4.2s；偶发 13–20s 尖峰是链路/colo 调度（曾见请求被分到 WAW）|
| 机房 IP 会被挑战 | 本机（机房出口）访问 transhelper.org 会吃 403 挑战；真实用户不受影响 |
| 免费额度边界 | D1 写 10 万行/天（匿名检索 **3 行/次** ≈ **3.3 万次/天**）、读 5M/天；Workers 免费版 CPU 10ms/128MB/50 子请求 |

---

## 2. 目标 / 非目标

**目标**
1. **有真实用户在用**，且反馈闭环（每条反馈有结论）。
2. **性能与限流手感有真实数据**（把 `plan.md` 里的预估表格回填成实测）。
3. **成本可见可控**（每月账单 + 每次检索成本模型）。
4. **对外可解释**（`/about`、隐私说明、**医疗免责**）。
5. **故障可演练**（key 挂、上游挂、被刷、被墙都有明确动作与验证）。

**非目标**
- 不做商业化、不做多地域部署、不做用户增长运营。
- 不新增大功能（AI 伴读/检索/配额已是完整闭环）。
- 不追求 SLA（免费层无告警能力，先做"发现即止血"）。

---

## 3. 工作包

### W1 · 小圈子内测与反馈闭环（核心）

| 项 | 内容 |
|---|---|
| **招募** | 跨性别社群内小范围邀请（10–15 人）；明确说明：**墙内需匿名使用、AI 伴读要登录且需能访问 x.com**、内容仅供参考非医疗建议 |
| **渠道** | ① GitHub Issues（模板：检索质量 / 功能建议 / 隐私与安全）；② 站内"反馈"入口（**可选**，一行链接到 Issues 即可，避免做表单与存储）|
| **闭环** | 每条反馈 → 分类（bug / 质量 / 建议 / 无效）→ 结论（修复 / 排期 / 不采纳+理由）→ 记入 `tasks.md` 或关闭 |
| **验收** | 内测满 **1 周**，**≥5 名真实用户**完成 ≥1 次检索与 1 次 AI 总结；**所有反馈条目有结论** |
| **谁做** | 你（招募/沟通）+ 我（技术处理）|
| **预估** | 招募 1 天；处理随反馈量 |

> ⚠️ **合规与伦理前置**：本 wiki 语料涉及 HRT/用药，AI 总结必须带免责说明（见 W5），内测沟通里也要说清"不能替代就医"。

### W2 · 压测复跑（含限流手感验证）

| 项 | 内容 |
|---|---|
| **检索性能** | 20 条真实 query（覆盖 HRT/用药/心理/手术/法律等主题）×3 轮，记录 `timings`（embed/search/rerank/total）的 P50/P95；`use_reranker` 开/关各一组，**回填 `plan.md` 性能表** |
| **限流手感（重点）** | ① 正常用户节奏（如 10 次/分钟连续 5 分钟）**不应**触发突发封禁；② 模拟"用户着急连点"（10 秒 20 次）→ 确认会硬封 60 秒并给出**清晰提示**；③ 若判定误伤，调 `BURST_PER_10S`（env，30–40）|
| **分档验证** | 用境外 IP（我方）验 `overseas` 档；CN 三档需 CN 出口 IP（内测用户代跑或临时调低阈值验证逻辑）|
| **额度边界** | 压一轮看 D1 写入量（匿名 3 行/次）与 Workers 子请求数；记录"安全日活上限" |
| **验收** | 性能表回填真实数字；**突发阈值有明确结论**（保留 20 还是调高，附理由）；D1/KV/Workers 用量有数 |
| **谁做** | 我（脚本+跑）；你（CN IP 实测协助）|
| **预估** | 半天 |

### W3 · 成本与月账

| 项 | 内容 |
|---|---|
| **成本模型** | 每次检索（embed + rerank）≈ ? 元；每次 AI 总结（tokens_in≈2.3k / out≈0.5k）≈ ? 元 —— 用真实单价 × 实测 token 算，写进 `plan.md` §8.4 |
| **月账** | 硅基流动（embedding/rerank/LLM）、Cloudflare（Workers/Pages/D1/KV/Queues）、Qdrant Cloud、域名；记入 `history.md` 或独立账目页 |
| **上限护栏** | 明确"每月可承受上限"，与 W2 的日活上限对齐；触线时先收紧哪一档（顺序：境外/机房 → CN 其它 → CN 家宽）|
| **验收** | 有一张"用量 → 费用"的表；对下月用量给出预测与上限动作 |
| **谁做** | 我（拉数、建模）；你（账单核对）|
| **预估** | 2 小时 |

### W4 · 运营面板补全

现状 `/admin` 已有：用量/配额、keys、封禁/加额、审计、分档限流观测、ingest runs。
缺口与建议：

| 缺口 | 方案 | 优先 |
|---|---|---|
| **日活 / 检索量趋势** | 不新增每请求写入：由 `quotas.requests`（登录）+ `rate_counters`（匿名，仅当前窗口）近似；若要长期趋势需新表，**先不做**，改为每周人工看 `/admin/ratelimit` | P2 |
| **key 余额 / 补货告警** | `/admin/keys` 已有池健康；补一条**余额探测**（可选：定时探测上游余额），不足时通知 | P1 |
| **摄取历史可视化** | `/admin/ingest/runs` 已有数据，前端补一个列表（含 `files_updated/points_upserted/status`）| P1 |
| **告警（免费版无内置）** | 用 **GitHub Actions 定时探活**：每 30 分钟打 `/healthz` + 一次真实检索，失败则开 Issue / 发通知 | P1 |
| **反馈条目面板** | 不做（用 GitHub Issues）| — |

**验收**：`/admin` 能回答"现在健康吗、摄取昨天成功了吗、key 池够吗、今天被刷了吗"。

### W5 · 对外文档与合规（**含医疗免责**）—— ✅ **已完成（2026-09-11）**

> 落地：`/about` 页（数据来源 / 工作原理 / **AI 医疗免责置顶** / 隐私逐条对齐实现 / 额度与限流 / 开源致谢 / 反馈渠道）、
> AI 卡片底部常驻免责行、页脚「关于与免责」入口、登录页隐私段指向 `/about`。
> 线上验证：`https://search.chengxi.moe/about/` 200，关键句全部在页面里。
> 隐私声明的事实依据与"改代码要同步改声明"的约束已记入 `history.md` 坑 41–42。
>
> **追加（用户反馈后，2026-09-11 当天完成）**：免责声明不再只藏在页脚 ——
> ① **默认弹窗**（`DisclaimerDialog`，全站首次访问即弹）：主按钮「我知道了，不再提示」= `ack()`、
> 次按钮「本次关闭」= 仅本次会话；**Esc = 本次关闭、点遮罩不关闭**（同意必须显式，避免误触代签）；
> ② **记住方式双写**：localStorage `prism_disclaimer_ack`（值 = 版本串 `v1`）+ 已登录时写账号
> `accounts.disclaimer_ack_at`（换设备不再弹）；**文案大改时把 `DISCLAIMER_VERSION` 改成 `v2` 即让所有人重看一次**；
> ③ **搜索框正下方常驻合规小字**「继续使用即表示你已阅读并同意 免责声明与使用须知」→ `/about`；
> ④ `/settings` 新增「启动时显示免责提示」开关；⑤ `/about` **底部**再加返回按钮。
> 后端：`GET /me` 增 `disclaimer_ack_at`/`disclaimer_ack`，新增 `POST /api/v1/me/disclaimer`（401/422/404/503 全测）。
> 线上已验证：`/me` 初值 null/false → ack=true 写入时间戳 → ack=false 清空；前端产物里弹窗**只在 JS、不在任何 HTML**（SSR 门控，不闪）。


| 项 | 内容 |
|---|---|
| **`/about` 页** | 项目定位、数据来源（四库 wiki）+ 许可（GPL-3.0-or-later）、技术栈、**隐私说明**（只存 `sha256(x_id)`、不收集手机/邮箱/实名、不存检索历史）、致谢 |
| **医疗免责（必做）** | AI 回答 **不是医疗建议**；用药/剂量以 wiki 原文与医生为准；AI 可能出错（幻觉），**引用卡片可点回原文**。落点：AI 卡片下方常驻一行 + `/about` + 内测沟通 |
| **AI 出错提示** | 前端 AI 卡片底部加"内容由模型生成，可能有误，请以引用原文为准"（已有引用回跳，缺一句明示）|
| **检索质量说明** | 说明混合检索 + 重排、为何登录才有 AI、额度过期规则（5h 网格锚定）、限流档位与"被限流时怎么办" |
| **验收** | `/about` 上线；AI 卡片有免责行；隐私说明与代码行为逐条对齐（尤其"不存什么"）|
| **谁做** | 你（口径）+ 我（实现）；文档措辞需你确认 |

### W6 · 应急预案与演练（tasks.md 明确要求"key 挂单演练"）

| 场景 | 动作 | 演练方式 |
|---|---|---|
| **单把 key 挂了** | Key Pool 自动换 key、连挂进冷却；`POST /admin/keys` 下架 | **真做一次**：`POST /admin/keys {enabled:false}` 下架 `llm-key-0` 观察自动切换，再恢复（KV `keydeny` 即时生效）|
| **LLM 池全灭** | 降级为纯检索 + 「AI总结暂不可用」 | 临时把 `LLM_POOL_KEYS` 指向无效值（CF var）验证降级，再改回 |
| **embedding 池全灭** | 自动回退 Qdrant 全文索引 | 同上思路，用无效 key 验证回退仍在工作 |
| **Qdrant 挂** | 检索降级/报错；`/healthz` 可探 | 探活脚本覆盖；不做破坏性演练 |
| **被刷** | 分档限流 + 突发封禁 + 全局熔断（必要时收紧 env）；极端情况上 Turnstile | 用脚本复现一次"1 分钟打满"，确认止血时间与观测可见 |
| **域名被墙** | 切到 `search.transhelper.org`（前端已同源复用同一套 `/api`）| **演练一次切换**（改 `FRONTEND_BASE_URL`? 不需要——两个域名都可用，只需对外告知备用域名）|
| **验收** | 每个场景有"操作步骤 + 观测点 + 恢复步骤"，并至少真跑 2 项（key 挂单、LLM 全灭）|

### W7 · 阈值与清单校准（技术债 #7）

| 项 | 内容 |
|---|---|
| **ASN 清单** | 按真实流量校准 `src/data/cn-asn.ts`：从 `/admin/ratelimit` 的档位分布 + 内测用户 ASN 采样，补充/剔除；**不存 ASN**，用一次性采样脚本 |
| **限流阈值** | 按 W2 结论调 `BURST_PER_10S` / 各档 env |
| **配额权重** | 按真实 token 消耗复核 200/100/真实 token 是否合理（默认 30 万 token/窗口）|
| **验收** | 清单与阈值都有"基于什么数据调的"记录（写进 `plan-ratelimit.md` 或 `history.md`）|

---

## 4. 验收标准（M4 出关）

1. **内测**：≥5 名真实用户、满 1 周；反馈条目 100% 有结论（修复/排期/不采纳+理由）。
2. **性能**：`plan.md` 性能表回填实测 P50/P95（rerank 开/关）；突发阈值有明确结论。
3. **限流**：正常用户节奏不误伤；连点触发的硬封禁有清晰中文提示与倒计时。
4. **成本**：有"用量 → 费用"表 + 每次检索/每次总结成本 + 月上限动作。
5. **运营**：`/admin` 能回答健康/摄取/key/被刷四问；探活脚本按 30 分钟跑通并能在故障时通知。
6. **文档与合规**：`/about` 上线；AI 卡片有医疗免责与"可能出错"提示；隐私说明与实现一致。
7. **演练**：key 挂单、LLM 全灭**真跑过**；其余场景有书面步骤。
8. **回归**：全套单测保持全绿（当前 **573**）；`tsc --noEmit` 0 错。

---

## 5. 排期建议（个人运营节奏，可按实际情况伸缩）

| 批次 | 内容 | 预估 |
|---|---|---|
| **第 1 批（先做）** | W5 免责与 `/about`（**合规优先**）、W6 的两项演练、W2 压测复跑 | 1–1.5 天 |
| **第 2 批** | W1 招募 + 内测启动（边收反馈边做）| 1 天 + 持续 |
| **第 3 批** | W3 月账、W4 面板与探活 | 1 天 |
| **第 4 批** | W7 校准（等内测有量再做）| 半天 |

---

## 6. 风险与开放问题

1. **墙内体验缺口（最大产品风险）**：X 登录被墙 → 墙内用户**用不了 AI 伴读**。M4 需要明确对外口径；若要补，得加一种墙内可用的登录方式（邀请码 / Passkey，均为新功能，超出 M4 范围 —— 单独议）。
2. **突发封禁的误伤风险**：20 次/10 秒会硬封 60 秒。真实用户"着急连点"是否合理触发，需要 W2 的数据支撑；阈值可调。
3. **成本失控风险**：匿名检索可用（`REQUIRE_LOGIN=0`），成本闸门是分档限流。若被刷且未及时收紧，可能烧额度 → W4 的探活与 W2 的上限数据是护栏。
4. **医疗内容责任**：语料与 AI 输出涉及用药，免责说明是**必做项**（W5），不能延后。
5. **免费层限制**：D1 写 10 万/天、Workers CPU 10ms；达到上限时需付费（$5/月）或降级（收紧匿名）。
6. **单人维护**：所有运维动作要尽量脚本化（探活、月账、演练步骤都写清），避免"只有我知道怎么修"。
7. **Qdrant 容量**：需核对当前 1481 chunk 的实际占用与套餐边界（W2 顺带记录）。

---

_创建：2026-09-11 · 待评审后拆任务执行_
