# plan-topk.md —— 用户可选返回条数（1–50）+ 成本与过采样联动（设计稿）

> 状态：**待评审**（2026-09-11 起稿）
> 关联：`plan-ratelimit.md`（分档限流与配额）、`plan.md` §5.2（rerank 与候选裁剪）、`history.md` 坑 43（成本实测）
> 本文只描述设计；评审通过后拆任务实施。

---

## 1. 现状（代码实测，2026-09-11）

| 项 | 现状 |
|---|---|
| `top_k` 取值范围 | **1–30**（`validate()`：越界抛 `invalid-top-k` → 422）|
| `top_k` 默认 | **10** |
| 前端 | `SearchBox.vue` **写死 `top_k: 10`**，用户不可选 |
| rerank 候选数 | `rerankCandidateLimit()` = `env.RERANK_TOP_K` 覆盖，否则 **`topK × 3`**（即 n=10 → 30 条候选）|
| 配额成本 | **固定**：search 200 / +rerank 100（与 n 无关）|
| 限流 | 分档：CN 家宽 30 / CN 其它 15 / CN 机房 6 / 境外 10 / 登录 60（次/分钟）；**与 n 无关** |
| AI 总结用的命中数 | **≤ `LLM_MAX_HITS` = 6 条**（每条 ≤600 字）→ **n 超过 6 不增加 LLM 成本** |

---

## 2. 可行性结论

**可行**，而且改动集中在四处：`validate()` 边界、候选数计算、`computeQuotaCost()`、前端选择器。
rerank 过采样本身是检索质量的标准做法（现状 3× 已经在做），你提的 2× 只是把系数调小。

但有 **两处理论问题**必须先说清，否则成本模型会"看起来合理、实际不合理"：

### 🔎 问题一：真实成本**并不随 n 线性增长**
| 环节 | 是否随 n 增长 | 说明 |
|---|---|---|
| query embedding | **否** | 每次搜索只 embed 一次查询词 |
| Qdrant 向量检索 | **几乎否** | 同一批候选里取前 n 条，差别只是回传 payload 大小 |
| **rerank** | **是** | cross-encoder 的代价 ∝ (query × 候选条数)，候选数我们控制 |
| AI 总结 | **否** | 只取前 6 条（`LLM_MAX_HITS`）|

→ 也就是说：**纯检索取 50 条 vs 10 条，我们的上游花销几乎一样**。
**建议仍然按 n 线性计费** —— 但理由不是"成本高"，而是：
> **防批量抓取**。语料是本站最有价值的资产；n=50 时单次请求能一次拿走 5 倍内容，
> 线性计费让"抓库"的性价比不随 n 提升（配合未登录上限 5 条，匿名抓取更不划算）。

这一点请你在评审时确认（若你只想"按真实成本计"，那 n 对纯检索就不该计费，只有 rerank 候选数才计）。

### 🔎 问题二：小 n **不该放大**限流
你提的是"n=1 → 限额也按比例放宽"。但每次请求的**固定成本**（embed ~0.65s + Qdrant ~0.65s，见坑 43 实测）
与 n 无关 —— 把 n=1 的限流放宽 10 倍，等于允许 **10 倍的请求数 × 同样的固定成本**，上游压力反而**上升**。

**我的建议（两种模型二选一，见 §3.3）**：
- **模型 A（推荐）**：**限流管"请求数"，配额管"资源量"** —— 限流仍与 n 无关；配额按 n 线性扣。
  n=50 时请求数不变，但额度 5 倍速度耗尽 → 天然限制总量，且不会放大上游请求数。
- **模型 B（你原方案）**：限流按 `10/n` 反向缩放（n=50 → 12 次/分钟；n=1 → 放宽）。
  简单直观，但 **n=1 时会放大请求数**（建议加"只缩不放"的保护：`max(1, 10/n)` 且不低于档位原值）。

---

## 3. 设计

### 3.1 取值范围与匿名限制
| 项 | 值 |
|---|---|
| 允许范围 | **1–50**（`validate()` 上限从 30 提到 50）|
| 默认 | **10**（保持现状，避免破坏既有前端与缓存 key 语义）|
| **未登录** | **上限 5 条**（超出 → **夹到 5** 并回 `warnings: ["top-k-clamped-anon"]`，不报错；前端把选择器限制在 1–5 并提示"登录后可到 50"）|
| 登录 | 1–50 |

> 选择"夹取 + warning"而不是 422 的理由：手写客户端不会因为多要几条而彻底失败，同时前端能明确告知；
> 与现有 `fallback` / `login-required` 的 warning 风格一致。

### 3.2 rerank 过采样规则
现状是 `topK × 3`；改为可配置的**过采样系数 + 上限**：
```
candidates = clamp(ceil(n × RERANK_OVERFETCH), n, RERANK_MAX_CANDIDATES)
```
| env | 默认 | 说明 |
|---|---|---|
| `RERANK_OVERFETCH` | **2** | 你举的例子（n=5 → 取 10）就是 2×；现状硬编码是 3× |
| `RERANK_MAX_CANDIDATES` | **60** | n=50 → 100 会太贵，封顶 60 |

举例：n=5 → 10；n=10 → 20；n=30 → 60；n=50 → 60（封顶）。
**候选池 = rerank 的输入，用户拿到的永远是 rerank 后的前 n 条**（质量优于"只 rerank n 条"）。
未登录 + 开 rerank 且 n=5 → 取 10 条候选 ✓ 与你描述一致。

### 3.3 成本与限流（两模型对照，取值待你拍板）

以默认 n=10 时"纯搜索 200 token / +rerank +100"为基准，定义 `UNIT = 20`（= 200/10）：

| n | 纯搜索成本 | 开 rerank 成本（模型 A）| 每 5h 窗口可搜次数（纯搜索 / 开 rerank）|
|---|---|---|---|
| 1 | 20 | 40 | 15000 / 7500 |
| 5 | 100 | 200 | 3000 / 1500 |
| **10（默认）** | **200** | **400** | 1500 / 750 |
| 20 | 400 | 800 | 750 / 375 |
| 50 | 1000 | 2000 | 300 / 150 |

- 纯搜索成本 = `UNIT × n`（n=10 时与现值 200 完全一致 → **存量用户无感**）
- 开 rerank 后**总量翻倍** = `2 × UNIT × n`（对应你说的"开 reranker 消耗倍数再翻倍"；
  现状是 1.5×（200+100），改成 2× 属于**收紧**，需你确认）
- 表里按 `QUOTA_WINDOW_TOKENS=300000` 估算

**限流（模型 A，推荐）**：`limitForTier` 不变 —— 限流只管请求频率；n 的差异全部通过配额体现。
**限流（模型 B，你的原方案）**：`effectiveLimit = ceil(tierLimit × 10 / n)`，并加保护 `≥ tierLimit` 不放大（只缩不放）。

### 3.4 前端交互
1. `SearchBox` 增加**条数选择器**（1–50，默认 10）：下拉或滑杆（含数值显示）。
2. **实时成本提示**：显示"本次约消耗 X 点额度（默认 10 条的 Y 倍）+ 开 rerank 再翻倍"，让用户看到代价。
3. 偏好持久化进 `usePrefs`（与 corpora/rerank/llm 开关一致）。
4. **未登录**：选择器上限 5，并在控件旁提示"登录后可返回最多 50 条"。
5. 结果区显示"共返回 N 条"，命中数不足时如实显示（例如只要 20 条但库里只匹配到 12 条）。

---

## 4. 实施步骤

| # | 内容 | 文件 | 预估 |
|---|---|---|---|
| 1 | `validate()` 上限 30→50；新增**未登录夹取 5** 与 warning 通路 | `src/search.ts`、`src/index.ts` | 1h |
| 2 | 过采样规则：`RERANK_OVERFETCH` / `RERANK_MAX_CANDIDATES` 替换硬编码 `×3` | `src/search.ts`、`src/types.ts` | 1h |
| 3 | 成本模型：`computeQuotaCost({ topK, rerank })` = `UNIT×n`（+ rerank 再 ×2）| `src/quota.ts` | 1h |
| 4 | （若选模型 B）闸门支持按 n 调限额 | `src/index.ts`、`src/ratelimit.ts` | 1h |
| 5 | 前端选择器 + 成本提示 + 未登录上限 + 偏好持久化 | `frontend/components/SearchBox.vue`、`pages/index.vue`、`composables/usePrefs.ts` | 2–3h |
| 6 | 测试与线上验收（见 §5）| `tests/**` | 2h |

---

## 5. 验收标准

1. **边界**：`top_k` = 0 / 51 / 1.5 / "abc" → 422 `invalid-top-k`；1 与 50 均可用。
2. **匿名夹取**：匿名 `top_k=50` → 实际返回 ≤5 条，且响应 `warnings` 含 `top-k-clamped-anon`；登录后同样请求返回最多 50 条。
3. **成本联动**（实测 `used_tokens` 差值）：n=10 纯搜索 = **200**；n=50 = **1000**；n=10 开 rerank = **400**；n=1 纯搜索 = **20**。
4. **过采样生效**：开 rerank 时，Qdrant 取回条数 = `clamp(2n, n, 60)`（用 `timings` 或日志断言），用户拿到 n 条。
5. **质量不回退**：同一组 query 下，n=10 开 rerank 的首条命中率**不低于**改动前（回归对照）。
6. **限流**：模型 A 下 n 不影响请求限额；模型 B 下 n=50 的限额为 `ceil(档位/5)` 且 n=1 **不放大**。
7. **前端**：选择器可用、刷新后记住；未登录上限 5 且有登录提示；结果区显示实际返回条数。
8. 全套单测保持全绿（当前 **606**）+ `tsc` 0 错。

---

## 6. 风险与开放问题

1. **成本模型是政策而非真实成本**（§2 问题一）：若你不认可"防抓取"这个理由，n 对纯检索就不该计费。
2. **限流是否反向缩放**（§2 问题二）：模型 A 简单且不放大上游请求数；模型 B 更符合你的原始描述但有放大风险。
3. **rerank 成本从 1.5× 改到 2×** 是收紧（默认额度下每窗口可搜次数从 1000 降到 750）—— 需你确认。
4. **n=50 的响应体积**：50 条 × 每条 ≤600 字 ≈ 30KB，可接受；但**缓存 key 含 top_k**，n 变化会各占一份缓存（命中率下降，属预期）。
5. **抓取防护的边界**：即便线性计费，登录用户仍可在额度内一次性拿 50×N 条；真要强防护需要"结果去重/限流模式/水印"等额外手段（**不在本次范围**）。
6. **`RERANK_OVERFETCH` 从 3× 降到 2× 会略微降低重排质量**（候选池变小）——先用同一压测集对照再定，必要时保留 3×。

---

_创建：2026-09-11 · 待评审（需确认 §2 两个问题与 §3.3 的取值）_
