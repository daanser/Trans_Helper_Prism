# plan-promo.md —— 开业酬宾：限时开放 DeepSeek V4.1 Flash（思考等级可调）

> 状态：**✅ 已实施并线上验证（2026-09-13）**——`promo.ts` + 独立池 + 促销链 + 4× 额度 + 预算闸 + 前端控件均已上线；
> 上线过程中**遇到并修掉两个真问题**（见 §11 事故记录），最终实测：促销模型真实生效（`model=deepseek-flash`）、
> 记账精确（D1 `pool='ds'` 的 `cost` 与上游单价吻合）、思考开关有效（开=10s/关=6s）、零静默回退。
> 上游：用户提案（2026-09-12）：「放两个 tokenrhythm 的 68 元余额 key（`DS_POOL_KEY_0/_1`），限时登录用户用 deepseek v4.1 flash（id `deepseek-flash`），可调思考等级，登录用户显示 4 倍 5h 限额（约 1M），两个 68 元用完即止」

---

## 1. 目标与非目标

**目标**：给登录用户一段**限时**的高质量模型体验（DeepSeek V4.1 Flash，1M 上下文 / 最大输出 384K / 支持思考），
配额展示提升到 **4×（≈1M 加权 token / 5h）**，**两把 ¥68 key 用完即止**（自动关闭并回退到免费模型）。

**非目标**：
- 不做付费/充值体系；不面向匿名用户（**仅登录用户**，这是本次防滥用的第一道闸）。
- 不做"思考等级"的假开关 —— **上游若不能真分级，就不做这个 UI**（见 §3 与 §5.5，项目已有"对外声明必须与实现一致"的硬规矩）。

---

## 1.5 已定决策（2026-09-13 用户逐条确认，**以此为准**）

| # | 决策 | 值 |
|---|---|---|
| **A1** | **单价** | 输入 **¥2.00/M**、输出 **¥8.00/M**、**缓存命中 ¥0.04/M** |
| **A2** | 密钥位置 | CF Secrets **`DS_POOL_KEY_0` / `DS_POOL_KEY_1`**（本地放 `.dev.vars`）|
| **A3** | 谁能用 | **登录用户默认开启** —— 模型选择处**直接从 Qwen 变成 DeepSeek V4.1 Flash**（无勾选开关）|
| **A4** | 每用户上限 | **不设**。用户理由：1M/5h，就算连搜 50h 也只 10M；基元律动直转官方 API 且 **DeepSeek 缓存命中率高**，80% 命中下 10M 只要 ≈¥4.3 |
| **A5** | 期限 | **暂定 1 个月**（`ends_at`）|
| **A6** | 思考等级 | 若探测发现**不能真分级 → 只做「深度思考」开关** |
| **A7** | 输出上限 | **促销 4000**；**免费链从 800 提到 1000**（现在偶尔截断）|

### 预算核算（¥68 × 2 = **¥136**，按 A1 单价）

| 场景 | 每 1M 输入 | 每 1M 输出 | ¥136 能买 |
|---|---|---|---|
| 80% 缓存命中 | 0.8×0.04 + 0.2×2 = **¥0.432** | ¥8 | 粗算 **≈17M–70M tokens**（取决于输入/输出比与命中率）|
| 全部未命中 | ¥2 | ¥8 | 同上区间下沿 |

> **满负荷单人能烧多快**（诚实估算，供你判断 A4 的取舍）：一次总结 ≈ 2.5k 输入 + 0.7k 输出 ≈ ¥0.0066；
> 1M/5h 的配额 ≈ 285 次总结/窗口 ≈ ¥1.9/窗口；**一个持续打满的用户一个月约 ¥270** →
> 也就是说**单人理论上能在约 2.5 周内吃掉全部 ¥136**。
> 你已明确不设每用户上限，**故本次不加上限**；但会补一道**"烧钱速度"告警**（§5.6），让你在提前耗尽前知道。

---

## 2. 现状与可复用的机件（已核对代码）

| 事实 | 位置 | 对促销的意义 |
|---|---|---|
| chat provider 每次请求构造：`createChatProvider(env, db)`，端点/模型来自 `LLM_ENDPOINT` / `LLM_MODEL` | `src/index.ts:152-162`、`src/llm.ts:797+` | **第二 provider 的接入点**：把端点/模型/池参数化即可，不必新造一套 |
| 密钥来自**合并池** `POOL_KEYS_<n>`（前缀扫描，ref = `pool-key-<n>`） | `src/keypool.ts:31,150,198` | 促销密钥要**独立池**（不同上游、不同计费）→ 需要给 `parseMergedKeys` 加前缀参数，新增 `DS_POOL_KEY_<n>` |
| 配额上限 = `limitTokens(env)` = `QUOTA_WINDOW_TOKENS`（默认 300k） | `src/quota.ts:213-215` | 4× 要在**促销开启时**返回 `PROMO_QUOTA_WINDOW_TOKENS`（默认 1_000_000），结束后自动回 300k |
| **`LLM_MAX_TOKENS` 硬常量 800** | `src/llm.ts:35`（env 只能调低） | ⚠️ 促销想要长总结**必须把这里改成可配置**（否则 384K 的输出能力白给） |
| `key_usage` 记 `pool / endpoint / tokens_in / tokens_out / account_id` | `src/db/schema.sql` | 「用完即止」的**记账基础**：DS 调用单独记 pool，按真实 token 换算成钱 |
| 分档限流：LLM 限额 = 档位 ÷ `RATE_LIMIT_LLM_DIVISOR`（默认 5） | `src/tiers.ts` | 促销期**不必放宽**（防滥用主护栏仍是它 + 全局熔断） |
| 前端顶栏只显示「剩余 x% · N 小时后重置」 | `app.vue:289-312` | 4× 生效后百分比口径不变，但**要加一句"促销期额度已提升"**否则用户看不懂为何百分比不掉 |

---

## 3. ⚠️ 必须先做的探测（**先做这个，再定稿实现细节**）

促销的核心承诺是"**可调思考等级**"，但**同类参数在国内网关经常被静默忽略**——
本仓库已有实测结论（`history.md`/memory）：硅基流动的 `Qwen3.5-4B` **`thinking_budget` / `reasoning_effort` 一律被忽略，瞎编参数名也返回 200**。
所以**不能假设** TokenRhythm 支持分级，必须用**真实请求**探明：

| # | 探什么 | 判据（必须可观测，不能只看 200） |
|---|---|---|
| 1 | `POST {DS_ENDPOINT}/chat/completions` + `deepseek-flash` 是否可用 | 200 且 `message.content` 非空 |
| 2 | **哪个参数控制思考**：`reasoning_effort`(minimal/low/medium/high) / `enable_thinking`(true/false) / `thinking_budget`(数字) | **比较不同取值的真实差异**：① 响应是否有 `reasoning_content`（或等价的思考字段）② 首字节/总耗时差异 ③ `usage.completion_tokens` 中思考占的比例 —— **仅"参数被接受"不算支持** |
| 3 | 是否**真的分级**（而不是只有 on/off） | 用 `low` 与 `high` 各打 5 次，看耗时/思考 token 是否有**稳定单调差异**；无差异 = 只能做开关 |
| 4 | 流式是否可用、`stream_options:{include_usage:true}` 是否回 usage | 需要**真实 token 计数**才能做"用完即止"；不回 usage 就只能估算（要如实标注） |
| 5 | 上下文 / 最大输出 | 官方 1M / 384K，实测确认（我们要放开 `LLM_MAX_TOKENS` 到多少） |
| 6 | 免费额度/余额是否可查（如 `/user/balance`、billing 端点） | 有则做"余额监控"，无则**只能靠自记账**（§5.6） |
| 7 | 错误形态：余额不足 / 429 / 401 分别长什么样 | 决定"用完即止"的触发条件与回退链（尤其**402 或特定 code**） |

### ✅ 实测结果（2026-09-13，真实请求，key = `DS_POOL_KEY_0`，模型 `deepseek-flash`）

| # | 探什么 | 结果 |
|---|---|---|
| 1 | 可用性 | ✅ **可用**。连续 8 次调用 **0 失败**；空 body 会明确报 `BAD_REQUEST` |
| 2 | 思考参数 | ✅ **`enable_thinking`（布尔）是真开关**：`false` → `reasoning` 0 字、输出 25–67 tokens；`true` → `reasoning` 259–936 字<br>❌ **`thinking_budget` → 400 `UNKNOWN_FIELD`**（**这个网关严格校验未知字段**，不像硅基流动静默忽略）<br>⚠️ `reasoning_effort` **被接受但无实际效果** |
| 3 | 能否真分级 | ❌ **不能**。`low` 组 reasoning 382/356/363 字；`high` 组 391/476/465/349/397 字 —— **两组完全重叠**（high 甚至出现过比 low 更短的）→ **按 A6：只做「深度思考」开关** |
| 4 | 流式 + usage | ✅ 末块含完整 `usage`（含 `prompt_tokens_details.cached_tokens` 与 `completion_tokens_details.reasoning_tokens`）→ **记账可行且精确** |
| 5 | 上限 | ✅ `max_tokens` **4000 / 8192 均被接受**（促销 4000 无问题）；上下文官方 1M（未压测） |
| 6 | 余额查询 | ❌ **API key 打余额端点 401**（只认浏览器会话 Cookie）→ **"用完即止"必须自记账**（与 §5.6 一致） |
| 7 | 错误形态 | 坏 key → **401 `UNAUTHORIZED`**；坏模型 → **400 `MODEL_NOT_AVAILABLE`**；空 body → **400 `BAD_REQUEST`** |

### 🔥 另外三个直接影响实现的发现

1. **响应里带 `cost_cny`（请求级成本，人民币）与 `billing_pending`**，且金额与 A1 单价**完全吻合**
   （实测：9 输入 + 41 输出 → `¥0.00034600` = 9/1e6×2 + 41/1e6×8 ✓）。
   ⚠️ **但只有"已结算"时才有值**：未结算时 `cost_cny=0.00000000` + `billing_pending=true`。
   → **记账主口径仍用 usage 自算**（确定性），`cost_cny` 仅用于**对账/告警**。
2. **缓存确实生效**：同一前缀第二次请求 `prompt_cache_hit_tokens=384`（共 602）→
   实测单次真实总结成本 **¥0.0009–0.0017（不思考）/ ¥0.004–0.0055（思考）**。
   也就是说 **¥136 ≈ 8 万–15 万次总结**（不思考），比先前保守估算便宜一个数量级 —— 你的"不设每用户上限"更站得住。
3. **思考会把 `max_tokens` 吃光**：实测 `max_tokens=1000` 时思考占 **936** tokens（输出被挤到 64）。
   → 促销 `max_tokens=4000` 时，**开启思考要预留思考预算**；实现里对"开启思考"的请求单独给出更宽松上限或明确告知可能截断。

### 延迟实测（重要产品取舍）

| 模式 | 真实负载（~600 输入 tokens + 总结指令）耗时 |
|---|---|
| 不思考（`enable_thinking=false`） | **7.3 / 12.3 / 17.9 s** |
| 思考（`true`） | **12.1 / 18.5 / 26.2 s** |
| 对照：现有免费 `Qwen3.5-4B`（生产实测均值） | **≈6.4 s** |

→ **促销模型是"更强但更慢"**（尤其开思考时）。前端需要在等待态给出合理预期（现有流式本身可见，但建议在促销标识旁注明"该模型较慢"）。

---

## 4. 促销的开关与"限时"

| 项 | 设计 |
|---|---|
| 总开关 | **KV flag** `promo:ds`（`enabled` / `ends_at` / `budget_used_yuan`），热路径读（带进程内缓存，与 key 禁用集同一套做法）→ **紧急关闭不必重新部署** |
| 结束条件（任一满足即自动关） | ① 到 `ends_at` ② **自记账累计金额 ≥ 预算**（两把 ¥68 = **¥136**）③ 上游返回余额不足 ④ **管理员手动关**（KV flag） |
| 关闭后的行为 | **不影响任何现有功能**：AI 伴读自动回退到免费模型（`Qwen3.5-4B` 链），配额上限回 300k；前端促销标识消失 |
| 立即可观测 | `/admin/usage/summary` 增加"促销用量与剩余预算"一节；`/admin/whoami` 或新端点回促销状态 |

---

## 5. 设计

### 5.1 密钥与池（独立，不混进 `POOL_KEYS_<n>`）
- 新变量 **`DS_POOL_KEY_0` / `DS_POOL_KEY_1`**（一把一个，数字递增；与现有 `POOL_KEYS_<n>` 同约定）。
- 实现：把 `parseMergedKeys(env)` 泛化为 `parseMergedKeys(env, prefix = "POOL_KEYS_")`，ref 规则不变（`ds-pool-key-<n>`）。
- **为什么不复用 `POOL_KEYS_<n>`**：不同上游、不同计费与限额，混池会导致"硅基流动的 key 被拿去打 TokenRhythm"（必然 401），
  也会把两套用量混进同一个池统计里，让"用完即止"算不准。
- 池的空/满、禁用（KV `keydeny:ds`）、LRU 轮转、失败换 key 全部复用现有 `KeyPool` 逻辑。

### 5.2 Provider 与回退链
- 新增促销 provider 配置：`DS_ENDPOINT`（默认 `https://tokenrhythm.studio/v1`）、`DS_MODEL`（默认 `deepseek-flash`）。
- **回退链**：`deepseek-flash`（促销，扣钱）→ 失败/额度尽 → 现有免费链（`Qwen3.5-4B` → …）。
  即 **促销是"升级项"而不是"替代项"**：它挂了、钱花完了，用户仍能拿到免费模型的总结 ✓。
- `key_usage.pool` 记 `ds`（或 `promo`）以便区分与统计。

### 5.3 谁可以用、默认是什么
- **仅登录用户**（匿名完全没有这个模型；这也让"促销"成为登录的正向理由）。
- 两种可选交互（**待你选**，§7-Q3）：
  - **A 默认开启**：登录用户直接享受促销模型，无需任何操作；思考等级默认"标准"。
  - **B 需要勾选**：AI 卡上多一个「本次使用 DeepSeek Flash（酬宾）」开关，默认关。
- 无论 A/B，**都必须在 UI 上明示这是限时酬宾 + 会消耗真实预算**（诚实原则）。

### 5.4 配额 4×（登录用户）
- `limitTokens(env)`：促销开启时返回 `PROMO_QUOTA_WINDOW_TOKENS`（**默认 1_000_000**），否则 `QUOTA_WINDOW_TOKENS`（300k）—— 即用户看到的 4×。
- 窗口锚定/重置时刻逻辑**不动**（网格锚定到注册时间）。
- 前端：顶栏百分比口径不变，但在 AI 卡与 `/settings` 加一句「**酬宾期间额度已提升至 4×（≈1M/5h）**」；促销结束后这句消失。

### 5.5 思考等级（**形式由 §3 探测结果决定**）
| 探测结果 | 做法 |
|---|---|
**✅ 实测结论（§3）：只支持 on/off，且"分级"是假的** → **按 A6 做「深度思考」开关**：

| 项 | 决定 |
|---|---|
| 参数 | `enable_thinking`（布尔）—— **唯一有效**的思考控制 |
| 默认值 | **默认关闭**（不思考 7–18s、思考 12–26s）。**开关必须显眼**（用户明确要求）|
| 位置与样式 | AI 卡**可折叠主体内**、与「模型：」同一行区域，**不放搜索卡**；样式为**显眼的胶囊按钮**（primary 描边 + 图标 + 文案「深度思考」，开启时实底 primary），旁附一句短提示「更慢，但更深入」；**仅登录用户且促销开启时出现** |
| 持久化 | `usePrefs`（与其它偏好同套），请求里显式传参便于事后核对 |
| 不做的 | ❌ 「思考等级」四级选择器（实测无效果，做了就是假控件）；❌ `reasoning_effort` / `thinking_budget`（前者无效、后者直接 400）|

等级选择**持久化**（`usePrefs`，与其它偏好同套），并在请求里显式传参（便于事后核对）。

### 5.6 记账与"用完即止"（本次最关键的钱袋子）
- **真实 token（✅ 实测可行）**：请求带 `stream_options: {include_usage: true}`，流式**末块**会带回完整 `usage`
  （含 `cached_tokens` 与 `reasoning_tokens`）→ 落 `key_usage`（`pool='ds'`），**不需要估算**。
- **成本自算（主口径）**：`miss = prompt_tokens − cached_tokens`；
  `cost = miss/1e6 × 2 + cached/1e6 × 0.04 + completion_tokens/1e6 × 8`。
  **`cost_cny` 只用于对账**（未结算时它是 0 + `billing_pending=true`，不能当主口径）。
- **金额换算**：`花掉 = tokens_in/1e6 × 单价_in + tokens_out/1e6 × 单价_out`（**单价由你提供**，§7-Q1）。
- **闸门与护栏**（按 A4：**不设每用户上限**）：
  1. **全局预算**：累计金额 ≥ **¥136** → 自动关闭促销；
  2. **烧钱速度告警**（新增，替代"每用户上限"）：`/admin/usage/summary` 显示"已花 ¥X / 剩余 ¥Y / 按近 24h 速度预计还能撑 N 天"，
     并在**预计将在 `ends_at` 之前耗尽**时由 watchdog 告警（让你有时间决定是加钱还是提前收尾）；
  3. 上游余额不足/402 → 立即关闭促销。
- 关闭时前端明确提示「酬宾额度已用完，已切回标准模型」，**不静默降级**。
- ⚠️ 缓存命中价（¥0.04/M）意味着**记账必须识别缓存命中**：上游若在 `usage` 里给出 `prompt_cache_hit_tokens`（或 `prompt_tokens_details.cached_tokens`）就按其计价；
  拿不到就按**未命中价上估算**（宁可高估，提前收闸）。

### 5.7 防滥用（复用现有护栏，仅加"钱"这一层）
- 登录强制 + LLM 分档限额（档位 ÷5）**不变**；
- 促销调用同样计入滚动 5h 配额（1M 口径）→ 天然限制总量；
- 突发封禁与全局熔断照旧；
- ⚠️ 注意：**TokenRhythm 若按模型计 TPM（如硅基流动的 `code:50602` 那样），换 key 无效**，
  需靠退避 + 模型内回退（§5.2）——探测第 7 项会给出真实错误码。

### 5.8 前端呈现（遵守已定稿的 UI 约定）
- AI 卡 header 已有状态徽章 → 促销期间在**主体顶部**加一行弱样式小字：「**开业酬宾 · DeepSeek V4.1 Flash**（限时，额度用完即止）」，**不新增开关式控件**（避免"两处开关"老问题）。
- 思考等级控件放在 **AI 卡可折叠主体内**（与"模型："同一行区域），不在搜索卡里（搜索卡只放检索参数）。
- `/about` 增加一节「开业酬宾」：模型、期限、额度、用完即止、数据同样只发命中片段。

---

## 6. 影响面（文件）

| 文件 | 改动 |
|---|---|
| `src/keypool.ts` | `parseMergedKeys(env, prefix)` 泛化；新增 `ds` 池 |
| `src/llm.ts` | 促销 provider 工厂（端点/模型/池可配）；**`LLM_MAX_TOKENS` 改为可配（默认 800，促销可提高）**；思考参数透传 |
| `src/chat.ts` / `src/index.ts` | `/search/stream`、`/chat` 选择促销 provider + 回退链；带 `stream_options` 记真实 usage |
| `src/promo.ts`（新） | 促销状态读取（KV + 进程内缓存）、预算/每人上限判定、金额换算（纯函数可测） |
| `src/quota.ts` | `limitTokens()` 促销期返回 4× |
| `src/usagestats.ts` / `adminstats.ts` | 促销用量与剩余预算 |
| `src/types.ts` | 新 env 声明（`DS_POOL_KEY_*`、`DS_ENDPOINT`、`DS_MODEL`、`PROMO_*`） |
| `frontend/components/AiCompanionCard.vue` | 促销标识 + 思考等级控件 |
| `frontend/composables/usePrefs.ts` / `useApi.ts` | 思考等级偏好 + 请求字段 + 促销状态 |
| `frontend/pages/about.vue` | 「开业酬宾」一节 |
| `wrangler.jsonc` / `.dev.vars.example` | 新变量与默认值 |

---

## 7. 验收标准

1. **可用性**：登录用户可拿到 `deepseek-flash` 的流式总结；匿名用户**看不到也调不到**促销模型（422/403 且 UI 无入口）。
2. **可回退**：把促销 KV flag 关掉（或模拟余额不足）→ 下一次请求自动用免费模型，**用户无感失败**（只有一行"已切回标准模型"提示）。
3. **额度 4×**：促销期 `/me` 与顶栏口径为 1M 窗口；关掉促销后自动回 300k；窗口重置时刻口径不变。
4. **用完即止**：① 模拟累计金额 ≥ 预算 → 自动关闭 ② 每用户达上限 → 该用户回退、他人不受影响 ③ 两种都**不产生 5xx**。
5. **记账准确**：DS 调用在 `/admin/usage/summary` 里单独可辨；上、下游 token 与上游 `usage` 一致（抽查 ≤1% 误差）；估算路径明确标注。
6. **思考等级**：按 §3 的实测结论实现（真分级 / 开关 / 不做），**绝不做无实际效果的参数**；请求日志里能看到实际发出的参数值。
7. **请求体必须干净**：上游**严格校验未知字段**（实测 `thinking_budget` → 400 `UNKNOWN_FIELD`）→ 发送的 body 只含它认识的字段；错误映射：401 → 换 key 重试，400（`MODEL_NOT_AVAILABLE`/`BAD_REQUEST`）→ **不重试**，直接回退免费链。
8. **思考开关有效**：开/关两侧的 `reasoning_tokens` 必须明显不同（关闭时应为 0）——**用真实请求断言，不能只看"参数发出去了"**。
9. **输出上限**：促销请求 `max_tokens=4000`、免费链 `1000`（实测长总结不再截断；`finish_reason` 抽查不为 `length`）。
10. **预算可视**：`/admin/usage/summary` 能回答"已花多少 / 还剩多少 / 按当前速度还能撑几天"。
11. **不回归**：现有 649 用例全绿 + `tsc` 0 错；前端 typecheck/generate 通过；免费链路的响应与耗时不变。
12. **文档**：`plan-promo.md` 补齐 §3 实测结果；`README.md` 的 API/环境变量表更新；`history.md` 记一条（促销设计与"钱的护栏"）。

---

## 8. 实施顺序

| 步 | 内容 | 谁 | 前置 |
|---|---|---|---|
| **0** | **探测 §3 全 7 项**（用两把 key 打真实请求，产出实测表） | 我（需你先把 key 放进 `.dev.vars` 或 CF Secrets） | 你给 key |
| 1 | 定稿 §5.5（思考等级形态）与 §7 的取值（单价、上限、默认开启与否） | 你拍板 | 步 0 |
| 2 | 后端：泛化池 + 促销 provider + 回退链 + 记账 + 双上限 + 4× 配额 | 我（可派 subagent） | 步 1 |
| 3 | 前端：促销标识 / 思考等级 / 4× 提示 / `/about` 一节 | 我 | 步 2 |
| 4 | 线上验收（含"关掉促销"与"预算耗尽"两次演练） | 我 | 步 3 |

**预计**：步 0 半天；步 2–4 约 1–2 天（含测试）。

---

## 9. 需要你提供的（其余已答，见 §1.5）

| # | 事项 | 状态 |
|---|---|---|
| Q1–Q7 | 单价 / 密钥位置 / 默认开启 / 无每用户上限 / 1 个月 / 思考等级退化为开关 / 4000+1000 | ✅ **已定（§1.5）** |
| **Q9** | **「深度思考」默认开还是关？** | ✅ **已定：默认关闭，但开关按钮要做显眼**（用户原话「开启按钮显眼一点」）|
| **Q8** | ~~两把 key 还没到我这儿~~ | ✅ 已到位，探测已跑完（§3）| ⏳ 请在 `backend-cf/.dev.vars` 里加两行 `DS_POOL_KEY_0=...` / `DS_POOL_KEY_1=...`（该文件 gitignored），然后告诉我一声 → 我立刻开始**步 0 探测**。也可直接在对话里贴给我，但**我不写进任何仓库文件、报告里只打码**。CF Secrets 那两把也请同步加上（线上要用）|

## 10. 风险

1. **"思考等级"可能是营销话术**：本仓库已实测过同类参数被静默忽略。**先探测再承诺**是本次的第一原则；若不能分级就不做。
2. **钱被单人烧完**：靠"每用户上限 + 全局预算 + 配额 4× 仍有限"三层；但仍无法阻止**多账号**轮换 → 若发现异常，KV flag 一键关闭。
3. **记账误差**：流式若不回 `usage` 只能估算 → 预算判定会偏；届时**按估算的 80% 提前收闸**（宁早不晚）。
4. **上游 TPM/余额错误码未知**：探测第 7 项给出真实形态；未知前不写死判定逻辑。
5. **配额 4× 是全局 env 口径**：所有登录用户都会看到 1M 窗口 —— 若你只想让"用促销模型的人"享受 4×，
   需要按"是否走 DS"分流（实现更复杂，且与"额度是账号级"的现有语义冲突）。**默认按你说的"登录用户 4×"实现**。

_创建：2026-09-13 · 已实施并验证_

---

## 11. 上线事故记录（两个真问题，都值得记住）

### 事故 1：`DS_ENDPOINT` 写成基础 URL → 促销链 **100% 静默回退**（看不出来）
- **现象**：`/me` 显示 `promo.enabled=true`、额度已是 1M，前端也显示了酬宾标识 —— 但每次总结实际都是 **Qwen** 在答
  （SSE 里 `model:"Qwen/Qwen3.5-4B"` + `promo-unavailable` 通知）。**用户以为促销在跑，其实从未用上。**
- **根因**：本仓库 `LLM_ENDPOINT` 的历史约定是**完整路径**（默认值含 `/chat/completions`），
  而 `DS_ENDPOINT` 按上游文档惯例写成了基础 URL `https://tokenrhythm.studio/v1` → POST 打到 `/v1`
  → **404 `NOT_FOUND 接口不存在`** → 首块预检失败 → 静默回退。
- **修法**：新增 `normalizeChatEndpoint()`，**基础 URL 与完整路径都接受**（缺 `/chat/completions` 就补上）+ 回归测试。
- **教训**：**"静默回退"必须能被观测**。当时"有输出、无报错"看起来一切正常；
  是核对 SSE 里的 `model` 字段才抓到。→ 促销状态里应把"最近一次实际使用的模型"暴露出来（`/me.promo` 已有 model，但那是**配置值**，不是**实际值**）。

### 事故 2：CF Workers 出口访问上游 **超时/504** → 开了 **Smart Placement** 才通
- **现象**：修好端点后仍 100% 失败：Worker 内 `GET https://tokenrhythm.studio/` **6s 超时**、
  `POST /v1/chat/completions` 返回 CF 的 **504**；而**本地**打同一接口完全正常。
- **根因**：该站点源站对 **Cloudflare 出口**（当时所在机房）不可达（跨境链路问题，非代码问题）。
- **修法**：`wrangler.jsonc` 加 **`"placement": { "mode": "smart" }`** → Worker 落到 NRT（东京）后，同一调用
  **HTTP 200 / 2.7s / model=deepseek-flash** ✓；整体检索 TTFB 未变差（2.44s/2.89s）。
- **教训**：**"本地能通"不等于"线上能通"**。诊断要用 `GET /api/v1/admin/dsping`（从 Worker 内部发请求）；
  另外别让诊断端点自己超时（首版两 key × 25s 被 CF 掐成 502 → 改成单 key + 短超时）。

### 事故 4：促销链沿用 20s 超时 → 开「深度思考」后几乎必失败
- **现象**：思考关 = 正常；思考开 = 12–26s（偶发更久）→ 超 20s 被预检判失败 → 静默回退 Qwen。
- **修法**：`PROMO_TIMEOUT_MS`（默认 **60s**）+ **按是否思考分流**（不思考 25s 快速回退）；两条 LLM 路径都改。
- **验证**：修复后"思考=开"连续 5 次全部 `deepseek-flash`、0 回退。
- → 记入 `history.md` 坑 55。

### 事故 3（记账）：KV 预算累计比 D1 真值高 ~2×
- **现象**：KV `spent_cny=0.0469` vs D1 `cost_cny_sum=0.0248` → 促销会在**真实花费约一半**时被提前砍掉。
- **根因**：对账 `reconcileSpentCny` 原本**只往上修正**（"宁早不晚"），多算的值永远降不下来。
- **修法**：改**双向收敛**到 D1 真值（差异 <0.0001 元才跳过写）；保留 5% 收闸余量。
  ✅ 实测已收敛：`D1 真值 = KV = 0.024798`（第一次对账即写回，第二次 `kv_reconciled=false` 不再重复写）。
